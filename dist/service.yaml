apiVersion: v1
kind: Service
metadata:
  name: '${SERVICE_DATA@microservice-auth-config}'
  namespace: microservices-namespace
  labels:
    moduleid: microservice-auth-data
    version: 1.70.0-auth-config-info
spec:
  clusterIP: None
  ports:
    -
      port: '${INTERNAL_PORT_DATA@microservice-auth-config}'
      targetPort: '${INTERNAL_PORT_DATA@microservice-auth-config}'
  selector:
    app: '${STATEFUL_DATA@microservice-auth-config}'
